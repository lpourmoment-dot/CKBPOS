# Plan — Appliquer les corrections CKBPOS dans CKBPOS-PRO

## Corrections à appliquer (4 fichiers dans CKBPOS-PRO)

### 1. `scripts/startup-guard.js` — path resolution + SHA-256 non bloquant

**Ligne 52** : `path.dirname(app.getPath('exe'))` → `app.getAppPath()` avec fallbacks
**SHA-256** : Les fichiers modifiés ne bloquent plus, juste un warning log
**Fichier** : `C:\Users\CHRIST BLACK CKB\Desktop\CKBPOS-PRO\scripts\startup-guard.js`

Copier le contenu de `C:\Users\CHRIST BLACK CKB\Desktop\CKBPOS\scripts\startup-guard.js` (version corrigée).

### 2. `main.js` — guard déplacé dans `app.whenReady()`

**Lignes 2-3** : `checkStartupFiles()` est appelé avant `app.whenReady()` → doit être déplacé dans `app.whenReady()` avant `createWindow()`

**Fichier** : `C:\Users\CHRIST BLACK CKB\Desktop\CKBPOS-PRO\main.js`
- Ligne 2-3 : garder juste le `require`, supprimer l'appel `checkStartupFiles()`
- Ligne 234 (`app.whenReady()`) : ajouter le check avant `createWindow()`

### 3. `package.json` — `build.files` +`scripts/**/*`

**Lignes 40-49** : Ajouter `"scripts/**/*"` dans `build.files`
`src/main/**/*` n'est pas nécessaire (ce dossier n'existe pas dans PRO)

**Fichier** : `C:\Users\CHRIST BLACK CKB\Desktop\CKBPOS-PRO\package.json`

### 4. `database/db.js` — dialogue erreur au lieu de crash silencieux

**Ligne 19** : `const db = new Database(dbPath)` sans try/catch → doit avoir le même handler de sécurité que CKBPOS

**Fichier** : `C:\Users\CHRIST BLACK CKB\Desktop\CKBPOS-PRO\database\db.js`

Copier le bloc try/catch de `C:\Users\CHRIST BLACK CKB\Desktop\CKBPOS\database\db.js` (lignes 34-62).

### 5. `scripts/db-encryption.js` — backup horodaté + chiffrement désactivé

**Ligne 110** : `copyFileSync` → `renameSync` avec backup horodaté
**Ligne 121-124** : `encryptDbOnExit` → désactivé (return immédiat)

**Fichier** : `C:\Users\CHRIST BLACK CKB\Desktop\CKBPOS-PRO\scripts\db-encryption.js`

## Vérification

1. Vérifier que les 5 fichiers ont été modifiés
2. `npm run verify` dans CKBPOS-PRO pour confirmer la structure
3. `npm run dev` pour tester le démarrage
